<odoo>
  <record model="ir.actions.act_window" id="room_list_action">
    <field name="name">Rooms</field>
    <field name="res_model">sccc.room</field>
    <field name="view_type">form</field>
    <field name="view_mode">tree,form</field>
    <field name="help" type="html">
        <p class="o_view_nocontent_smiling_face">Create the first room
        </p>
    </field>
  </record>

    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="sccc.list">
      <field name="name">sccc list</field>
      <field name="model">sccc.sccc</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="sccc.action_window">
      <field name="name">sccc window</field>
      <field name="res_model">sccc.sccc</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="sccc.action_server">
      <field name="name">sccc server</field>
      <field name="model_id" ref="model_sccc_sccc"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="sccc" id="sccc.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="sccc.menu_1" parent="sccc.menu_root"/>
    <menuitem name="Menu 2" id="sccc.menu_2" parent="sccc.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="sccc.menu_1_list" parent="sccc.menu_1"
              action="sccc.action_window"/>
    <menuitem name="Server to list" id="sccc" parent="sccc.menu_2"
              action="sccc.action_server"/>
    -->
</odoo>